{
  "name": "earthref",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mantra-core": "^1.2.0",
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "react-komposer": "^1.3.0",
    "react-mounter": "^1.0.0",
    "react-simple-di": "^1.0.1"
  },
  "scripts": {
    "test": "npm run lint-only && npm run test-only",
    "lint-only": "eslint ./lib ./client ./server --ext .js --ext .jsx",
    "lint-fix": "npm run lint -- --fix",
    "test-only": "mocha .scripts/mocha_boot.js **/test/*.js --compilers js:babel-core/register",
    "test-watch": "npm run testonly -- --watch --watch-extensions js,jsx"
  }
}
